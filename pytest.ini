[pytest]
testpaths = tests
addopts = -q -m "not slow and not integration"
markers =
    slow: long-running performance checks
    integration: full streaming/auth/WS flows
asyncio_default_fixture_loop_scope = function
